# Entorno
NODE_ENV=production

# Servidor
PORT=3001
BASE_URL=https://admin.oempos.com

# JWT
JWT_SECRET=clave-secreta-ultra-segura
JWT_EXPIRES_IN=8h

# Base de datos SQL Server (ajustar según servidor)
DB_DIALECT=mssql
DB_HOST=localhost
DB_NAME=gustados
DB_USER=sa
DB_PASSWORD=tu_contraseña_segura
DB_INSTANCE=SQLEXPRESS

# Almacenamiento de imágenes
IMAGE_STORAGE=database
IMAGE_MAX_SIZE=5242880
IMAGE_ALLOWED_TYPES=image/jpeg,image/png,image/webp

# Configuración extra opcional
TIMEZONE=America/Argentina/Buenos_Aires
